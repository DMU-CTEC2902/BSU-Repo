@model IEnumerable<MovieWebSite.Models.DirectorModel>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DirectorName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DirectorBio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DirectorDOB)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DirectorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DirectorBio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DirectorDOB)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.DirectorModelID }) |
                @Html.ActionLink("Details", "Details", new { id=item.DirectorModelID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.DirectorModelID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
